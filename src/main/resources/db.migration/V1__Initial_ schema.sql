CREATE TABLE location
(
    id           PRIMARY KE   NOT NULL,
    city         varchar(255) NOT NULL,
    district     varchar(255) NOT NULL,
    town         varchar(255) NOT NULL,
    neighborhood varchar(255) NOT NULL,
    zip_code     varchar(255) NOT NULL,
);